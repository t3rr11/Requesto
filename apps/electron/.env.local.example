# Copy this file to .env.local to enable local dev simulation flags.
# .env.local is gitignored — never edit constants.ts directly for this.

# Simulate a backend startup failure (shows the error window).
# SIMULATE_BACKEND_FAILURE=true

# Simulate an update being available (shows the update notification UI).
# SIMULATE_UPDATE_AVAILABLE=true
