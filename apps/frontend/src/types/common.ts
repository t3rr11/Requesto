// NOTE: This file is for common types that are used across multiple modules.
// State store types should go in their respective module folders (e.g. store/collections/types.ts).

export type KeyValueRow = {
  key: string;
  value: string;
};
