{
  "name": "@requesto/cli",
  "version": "1.8.1",
  "description": "Run your Requesto collections from the terminal and CI pipelines",
  "type": "module",
  "main": "dist/index.mjs",
  "bin": {
    "requesto": "./dist/index.mjs"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/bin.ts",
    "build": "node scripts/bundle.mjs",
    "start": "node dist/index.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.1",
    "requesto-backend": "1.8.0",
    "requesto-engine": "1.8.0",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "publishConfig": {
    "access": "public"
  }
}
